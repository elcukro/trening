# CLAUDE.md – projekt „Trening”

Osobisty asystent treningowy (PWA na iPhone) dla jednego użytkownika przygotowującego się do wyjazdu rowerowego w Alpy (wrzesień 2027).

## Najważniejsze zasady
- **Najpierw przeczytaj** `README.md`, potem `docs/07-specyfikacja-aplikacji.md`, `docs/01-zasady-treningu.md` i przejrzyj `data/program.json`. Otwarte pytania i podjęte decyzje: `docs/OPEN_QUESTIONS.md`.
- **Treść planu pochodzi z `data/`** – nie hardkoduj treningów, ćwiczeń ani dat w komponentach. Zmiana planu = zmiana generatora `data/reference_generator.py` → `npm run data:generate` (regeneruje `program.json` i `calendar.json`) + podbicie `PROGRAM_VERSION`.
- Silnik planu (`src/engine/`) to czysty TypeScript bez zależności od UI; dla ustawień domyślnych musi zwracać dokładnie to, co `data/calendar.json` (test golden `src/engine/__tests__/golden.test.ts`). Preskrypcje siłowe są w `program.json → gym_prescriptions` (slot `wed` = Sesja A/C/core, `fri` = Sesja B).
- Silnik operuje na datach ISO (łańcuchy), bez obiektów Date ze strefą. „Dziś” w `Europe/Warsaw` liczy `src/lib/dates.ts`; parametr `?today=YYYY-MM-DD` w URL nadpisuje datę (testy, podgląd).
- UI po polsku, tygodnie od poniedziałku, jednostki metryczne, przecinek dziesiętny (`src/lib/format.ts`), mobile-first (iPhone, safe areas, elementy ≥ 44 pt), tryb jasny i ciemny (`prefers-color-scheme`).
- Sekrety (Strava, Wahoo, Supabase service role) wyłącznie po stronie serwera (Supabase Edge Functions). Nigdy nie commituj `.env`. Klucze Wahoo użytkownik ma w `~/code/wahoo-routes/.env` – nie kopiuj ich do repozytorium, poproś o wpisanie do sekretów Supabase.
- Offline-first: ekran Dziś, Tydzień, Biblioteka i tryb siłowni muszą działać bez sieci (program w bundle'u, Workbox precache, Dexie).
- Aplikacja nie jest narzędziem medycznym – komunikaty o żywieniu i chorobie mają charakter ogólny.

## Komendy
- `npm run dev` – serwer deweloperski (Vite)
- `npm run test` – Vitest (silnik planu; `npm run test:coverage` – próg 90% dla `src/engine`)
- `npm run lint` – oxlint (zamiast ESLint – domyślny linter szablonu Vite 8)
- `npm run typecheck` – `tsc -b` (TypeScript strict, `noUncheckedIndexedAccess`)
- `npm run build` – build produkcyjny PWA (`dist/`, własny service worker z `src/sw.ts`)
- `npm run e2e` – Playwright, Chromium z viewportem iPhone 14 (buduje i uruchamia `vite preview` na porcie 4173); zrzuty w `test-results/`
- `npm run data:generate` – `python3 data/reference_generator.py`
- `node scripts/icons.mjs` – regeneracja ikon PNG z `public/icon.svg`
- Projekt w chmurze: ref `kgllegvlnmchdvkkbitt` (podlinkowany), produkcja `https://trening-inky.vercel.app`. Ustawienia auth (site_url, redirecty) wypychane przez `supabase config push` z **minimalnego** config.toml zawierającego tylko `[auth]` (pełny plik z szablonu `init` nadpisałby ustawienia hostowane) – zawsze najpierw `supabase config diff`.
- `supabase db push` – wgranie migracji do podlinkowanego projektu (`supabase link --project-ref …`); `supabase migration new` może zawisnąć w tej sesji – twórz plik ręcznie z tym samym formatem nazwy

## Struktura
```
data/                 program.json (źródło prawdy), calendar.json (golden), generator, sprzęt, checklista
docs/                 dokumentacja 00–07 + OPEN_QUESTIONS.md
src/engine/           silnik: schema.ts (Zod), dates.ts, layout.ts (R14), calendar.ts (dni, R16, tabela sezonu),
                      zones.ts (strefy, resolveWorkout), nutrition.ts, load.ts (R8, e1RM), climb.ts, progress.ts (R11 effectiveLthr,
                      masa/trend R10, e1RM, objętość), plan.ts (DayPlan dla UI)
src/engine/__tests__/ golden.test.ts, scenarios.test.ts (scenariusze §10 specyfikacji)
src/data/             program.ts (import + walidacja Zod), rules.ts (zasady R1–R16 dla ludzi)
src/db/               Dexie (IndexedDB): index.ts (schemat v2: settings, kv, checkins, session_logs, set_logs, test_results, outbox),
                      repo.ts (zapisy z outboxem: check-in, logi, serie, testy, historia ćwiczeń)
src/sync/             supabase.ts (klient lub null bez env), auth.ts (magic link + kod OTP), sync.ts (push outbox → pull po server_updated_at, LWW),
                      useSync.ts (start, online, visibility, zmiany outboxa), strava.ts, wahoo.ts + useWahoo.ts (automatyczna wysyłka 7 dni)
src/app/              App.tsx (router, dolna nawigacja, lazy Postęp), useSettings.ts (ustawienia + kontekst silnika + testy → R11)
src/features/         today (Dziś: check-in, karta roweru + log, test, siłownia), calendar (miesiąc), week, season, library, settings (+ konto, kopia),
                      more, progress (masa, testy, kalkulator, objętość, siła), gym (tryb siłowni: sequence.ts, useRestTimer.ts, GymModePage.tsx)
src/ios/              uproszczony interfejs iOS (`/i`): ios.css, IosApp.tsx, dayState.ts, summary.ts, useIos.ts, useBriefing.ts, components/, screens/
supabase/             config.toml, migrations/…_init.sql (tabele, RLS, trigger allowed_emails, lww_guard) – instrukcja: docs/09-supabase.md
src/components/       ui.tsx (Card, Badge, Button…), StepTimeline.tsx (oś czasu stref)
src/lib/              dates.ts (Europe/Warsaw, formaty pl), format.ts, labels.ts, zones.ts (kolory)
e2e/                  Playwright smoke (viewport iPhone)
public/               ikony PWA
```

## Stan etapów
- Etap 1 (plan offline): gotowy – silnik + testy golden, ekrany Dziś / Tydzień / Sezon / Biblioteka / Ustawienia, PWA.
- Etap 2 (logi, Supabase, tryb siłowni, Postęp): gotowy w kodzie. Projekt w chmurze zakłada użytkownik wg `docs/09-supabase.md`; dozwolony e-mail wpisuje się do `private.allowed_emails` przez SQL Editor (nie w repo). Bez env `VITE_SUPABASE_*` aplikacja działa lokalnie.
- Synchronizacja: rekordy mają UUID z klienta i `updated_at` (zegar klienta); serwer odrzuca starsze zapisy (trigger `lww_guard`) i stempluje `server_updated_at`, po którym klient pobiera zmiany. Soft delete przez `deleted_at`.
- Lokalna weryfikacja SQL bez Dockera: `createdb trening_test` + stub schematu `auth` (patrz historia commitów), potem `psql -f supabase/migrations/…`.
- Etap 3 (Strava): gotowy i wdrożony 11.09.2026 – `supabase/functions/strava-oauth` (OAuth, status, sync, subscribe; callback publiczny ze stanem HMAC) i `strava-webhook` (GET challenge, POST zdarzenia, import w tle; **sekret w ścieżce** `/<webhookPathSecret>` zamiast publicznego adresu, zdarzenia `delete`/`deauthorized` potwierdzane w API Stravy), moduły `_shared/` (crypto AES-GCM/HMAC z service role, klient Strava). Migracja `20260911140000_strava.sql`: `strava_activities` + trigger agregujący jazdy dnia do `session_logs`. Klient: `src/sync/strava.ts`, sekcja Integracje, `StravaCard.tsx` (strefy z histogramu, przenoszenie na inny dzień). Deploy: `supabase functions deploy <nazwa> --no-verify-jwt --use-api`; typecheck: `deno check supabase/functions/*/index.ts`. Opis: `docs/10-strava.md`.
- Etap 4 (Wahoo): kod gotowy i wdrożony 11.09.2026 – `src/engine/wahoo.ts` (generator `plan.json`, czysty TS z testami), `supabase/functions/wahoo-oauth` i `wahoo-push`, migracja `20260911160000_wahoo.sql` (jedna wysyłka na dzień: unikat `wahoo_pushes(user_id, date)`), klient `src/sync/wahoo.ts` + `useWahoo.ts` (automat raz dziennie, dziś + 6 dni), przycisk „Wyślij na Wahoo” na ekranie Dziś. Format pliku wg `cloud-api.wahooligan.com/docs/plan-json-format.pdf`: blok `repeat` ma wartość „ile razy PO pierwszym przejściu” i nie może mieć własnych `targets`. Ustawienie `power_meter` (Ustawienia → „Mam miernik mocy”): włącza cele `ftp` z `power_zones_ftp_fraction` w planach Wahoo, FTP w nagłówku (`effectiveFtp`: ostatni test Wattbike albo szacunek) i waty w krokach na ekranie. Bez miernika `ftp` nie trafia do nagłówka – ELEMNT liczy TSS/IF tylko z mocy, a bez celów mocy przyjmuje IF 0,40 (stąd TSS 16/24/32 dla 1/1,5/2 h). **Plik planu wysyłamy jako surowy JSON w przesyłce multipart** – wbrew dokumentacji Wahoo, która mówi o base64 (base64 daje błąd parsowania). **ELEMNT nie obsługuje celów tętna w planach** (przypis w dokumentacji formatu, potwierdzone na urządzeniu przez adb) – dlatego zakres bpm wchodzi na początek nazwy interwału (`intervalLabel`). Plan wiążemy przez `workout[plan_id]` (pole `plan_ids` w odpowiedzi to lista alternatyw i pusta tablica jest normalna); po zapisie weryfikujemy powiązanie odczytem pierwszego dnia. Limity sandboxa Wahoo (25/5 min, 100/h, 250/dzień) wymuszają oszczędność: 2 zapytania na dzień, przerwanie wysyłki przy 429, jedna automatyczna próba dziennie. **Plan JSON buduje aplikacja, nie serwer** – żeby nie dublować silnika w Deno; uzasadnienie w `docs/11-wahoo.md`. Do uruchomienia brakuje sekretów `WAHOO_CLIENT_ID/SECRET` i adresu zwrotnego w portalu Wahoo.
- Etap 5 (adaptacja, sprzęt, wyjazd): reguły i ekrany gotowe 11.09.2026 – `src/engine/rules.ts` (applyOverrides dla swap/move/indoor/sick/downgrade/skip + warningsFor dla R1–R7, R9, R12, R13 + validateSwap dla R15), hook `src/app/usePlan.ts` (okno 15 dni wstecz i 8 w przód, bo R7 patrzy dwa tygodnie), `RulesCard` z propozycjami, zamiana dni w widoku Tygodnia, ekrany Sprzęt i Wyjazd (`data/gear_tasks.json`, `data/packing_list.json`, migracja `20260911180000_gear_trip.sql` z deterministycznymi kluczami). Powiadomienia Web Push gotowe: `src/sw.ts` (strategia `injectManifest`), Edge Function `push-send`, tabele `push_subscriptions` i `push_log`, harmonogram `pg_cron` + `pg_net` z sekretem `push_cron_secret` z vaulta. Opis: `docs/12-powiadomienia.md`. **Wszystkie pięć etapów specyfikacji jest zrealizowanych.**
- Wersja na komputer (≥ 1024 px, 20.09.2026): boczna nawigacja `SideNav` z paskiem sezonu (`App.tsx`), treść do 1600 px, dolny pasek ukryty (`lg:hidden`); Dziś w kolumnach 3/2, Tydzień jako 7 równych kolumn, Ustawienia i Postęp w dwóch niezależnych kolumnach, Biblioteka 2–3 kolumny. **Kalendarz** (`src/features/calendar/CalendarPage.tsx`, trasy `/kalendarz`, `/kalendarz/:YYYY-MM`): siatka miesiąca z jazdą, siłownią, znacznikami, statusem z `session_logs` i nadpisaniami; dane z hooka `useRangeView(from, to)` w `usePlan.ts`. **Przenoszenie jazd**: przytrzymanie kafelka (320 ms) „podnosi” trening, potem przeciągnięcie albo dotknięcie celu (`useDragMove.ts` – pointer events, bez HTML5 DnD, działa na iOS); cel to wyłącznie dzień „Wolne” (bez jazdy i bez siłowni) w tym samym miesiącu, walidacja `validateMove` w `rules.ts` (R15: dwa akcenty pod rząd, Sesja nóg < 48 h przed ciężkim dniem, przeszłość, R2 jako ostrzeżenie); zapis jako override `move`, cofnięcie pigułką „Przeniesione: … ✕”. Po przeniesieniu trzeba wysłać plan na Bolta. **Wysyłka na Wahoo idzie przez `planWindow` (silnik) z nadpisaniami z `plan_overrides`** – `pushPayloadFor` ładuje je z Dexie z marginesem 40 dni (drugi koniec przeniesienia); komponenty mające gotowy `DayPlan` używają `pushItemForDay`. Testy: `e2e/desktop.spec.ts`, `e2e/calendar.spec.ts`. Uwaga E2E: zawieszony `vite preview` na porcie 4173 daje fałszywe błędy – najpierw `lsof -iTCP:4173`.
- **Plan z 20.09.2026 wdrożony** (`PROGRAM_VERSION` 2026.09.24-3): fazy PREP 0–2, I 3–14, II 15–24. **Tygodnie 2–9 (22.09–15.11) to blok jazd bez siłowni** (tydzień 2 testowy: 3 spokojne jazdy; od tygodnia 3 pięć jazd) (pn wolne, wt Z2, śr akcent, czw wolne, pt Z2, sb długa, nd Z2; rozładowanie w tyg. 6; opis i uzasadnienie: `docs/13` § Zmiana 22.09.2026). Od tygodnia 10 (16.11) wraca rytm 3 jazdy + 2 siłownie z **akcentem w czwartek**; siłownia startuje od stopnia `intro` po siedmiu tygodniach przerwy. Dalej test `FTP_TEST` (20 min, moc) w tyg. 2, 14, 24; treningi `SS_2x12` i `Z2_FORCE`; `gym_only` w szablonie tygodnia; R1 przenosi akcent tylko na dzień z lekką jazdą. Fazy III–V bez zmian. Szczegóły i otwarte pytania: `docs/13-plan-z-20-09-2026.md`. Tabele w `docs/02` i `05` dla tygodni 1–24 są historyczne.
- **Cel od 21.09.2026: FTP pod 30 km/h przez 2–3 h** (Alpy jako horyzont, nie cel); plan usprawnień z todo: `docs/14-usprawnienia-todo.md`. Wdrożone pkt 0 i 1: strumienie ze Stravy (`_shared/metrics.ts` – czysty TS testowany Vitestem z `src/engine/__tests__/metrics.test.ts`; tabela `strava_streams`, metryki w `strava_activities`), `src/engine/analysis.ts` (TSS/IF, plan vs wykonanie, status dnia), `src/engine/baseline.ts` + tabela `baseline_entries` (karta „Punkt wyjścia” w Postępie z celem 30 km/h), `LoadCard` (TSS tygodniami). Pkt 3: `src/engine/power.ts` (propozycja FTP z jazd ≥ 3 % ponad aktualne, LTHR z drugiej połowy najlepszych 20 min, krzywa mocy, historia FTP/W/kg) → `FtpSuggestionCard` (Dziś + Postęp, odrzucenia w kv `ftp_dismiss:<id>`), `PowerCard`, strefy z watami w Bibliotece. Pkt 4: `src/engine/pmc.ts` (plannedTss z kroków, pmcSeries CTL 42 / ATL 7 / TSB, rampRate R7, weekTss) + `src/app/useLoad.ts` (TSS per dzień: Strava → RPE → plan) → `PmcCard` w Postępie, TSS w nagłówku Tygodnia. Pkt 5: `src/engine/report.ts` (rangeReport: godziny/TSS/jednostki/zgodność, wyniki dni done/modified/skipped/missed/extra) → `ReportPage` (`/postep/tydzien/:monday`, `/postep/miesiac/:YYYY-MM`), push `weekly` w niedzielę 19:00, etykieta „Klucz”/★ dla `day_type === 'key'` w Tygodniu i Kalendarzu. Pkt 6: `src/engine/weather.ts` (summarizeWindow, adviseRide z progami −10/−5/28 °C, clothingFor z `data/clothing.json`, fuelPlan) + `src/sync/weather.ts` (Open-Meteo z klienta, bufor 3 h w kv, lokalizacja `weather_location` i `ride_hours` w kv) → `BriefingCard` na ekranie dnia (propozycja „pod dachem” przez override `indoor`), `WeatherSettings`. Pkt 7: `wahoo-push` tryb `completed` (`_shared/wahoo_summary.ts`, tabela `wahoo_workouts`, trigger `rebuild_bike_log_from_wahoo` – Strava ma pierwszeństwo), `wahoo_pushes`/`wahoo_workouts` w Dexie tylko do odczytu (`UPDATE_ONLY_TABLES` z pustą listą), `WahooStatus.tsx` (stan na Bolcie wg `external_id`, wykonane wg Bolta), automat `wahoo.completed(3)` po dziennej wysyłce. Pkt 8: zdjęcia ćwiczeń (start/koniec) z free-exercise-db (Unlicense) w `public/exercises/<id>-0.jpg`/`-1.jpg` (480 px; mapowanie `photo_source` w `data/exercises_media.json`, tam też link YT jako wyszukiwanie lub `video_id`, `bar_kg`, `note`), `src/data/exercisesMedia.ts`, `ExerciseMedia.tsx`; `platesFor`, `detectPlateau` w `src/engine/load.ts`; timer przerwy: wibracja + powiadomienie z SW gdy karta ukryta. Pkt 9: `src/engine/cadence.ts` (histogram, kadencja w strefach, trend tygodniowy, ostrzeżenie < 75 rpm × 2 tyg.), trening `Z2_FORCE` (wtorki tyg. 8/12/16/18/20/22, w `SCALABLE_WORKOUTS`), `cadence_in_target_pct` w `matchSteps`, `CadenceCard`. **Plan usprawnień 0–9 zrealizowany w całości (22.09.2026).** Szczegóły: `docs/10-strava.md` § Strumienie.
- **Wiele programów treningowych (24.09.2026)**: `src/data/program.ts` trzyma rejestr (`alps2027`, `ftp300`), a `settings.program_id` (kolumna `profiles.program_id`) mówi, który obowiązuje dane konto. `useSettings` czyta identyfikator z zapisu użytkownika, nie z domyślnych ustawień programu. Program o stałej długości deklaruje `layout: {"mode":"fixed"}` (tygodnie po kolei od `program_start`, tydzień 0 = pierwszy tydzień, bez R14) i może używać poniedziałku (`WeekTemplate.mon`; brak pola = wolne) oraz własnego `bike_default`. **Ustalenia jednego zawodnika nie mogą wyciekać na drugiego** – biblioteka treningów powstaje w `build_library()` z sekcji PARAMETRY OSOBISTE (`CAD_EASY`, `Z2_NOTE`, …) o wartościach standardowych; program alpejski nadpisuje je w swoim `main()`. `npm run data:generate` uruchamia oba generatory. Opis i ograniczenia: `docs/17-program-ftp300.md`.
- **Decoupling v1 (`docs/18-decoupling-v1.md`)** – plan w 6 krokach, żeby żadne założenie jednego zawodnika nie działało na koncie drugiego. **Krok 1 zrobiony (25.09.2026):** program ma wymagane sekcje `meta`, `nutrition`, `bikes`, `goal`, `cadence`; silnik nie ma własnych wartości domyślnych (`nutritionFor(policy…)`, `bikeSuggestion(bikes…)`, `lowCadenceWarning(trend, policy)`, `programGoal`). Opcjonalne `nutrition.weight_based`: deficyt liczony z masy obecnej (ostatni check-in → `EngineContext.current_weight_kg`) i docelowej do daty celu, z limitami. **Przy każdej zmianie w generatorze alpejskim `cmp` calendar.json przed/po – ma być identyczny, chyba że zmiana planu jest zamierzona.**
- **Uproszczony interfejs iOS (`/i`, 23.09.2026)**: drugi wygląd tej samej aplikacji, w stylu natywnej aplikacji na iPhone'a – cztery zakładki (Dziś, Tydzień, Postęp, Więcej), jeden wyróżniony krok dnia (check-in → Bolt → trening → podsumowanie), arkusze zamiast formularzy w treści. Kod w `src/ios/` (własny arkusz `ios.css` z systemowymi kolorami i skalą SF, ikony kreskowe zamiast emoji); logika kroków dnia w `src/ios/dayState.ts` (czysty TS, testy). Funkcje zaawansowane (kalendarz z przenoszeniem, wykresy, biblioteka, sprzęt, wyjazd, integracje, kopia) zostają **wyłącznie** w pełnej aplikacji `/` – przy dodawaniu czegoś do `/i` najpierw sprawdź, czy to jeden z pięciu codziennych kroków. **Domyślnie `/` przekierowuje do `/i` poniżej 1024 px** (na komputerze zostaje pełna aplikacja); `ui_mode` w `kv` nadpisuje to w obie strony (`ModeGate` w `App.tsx`). Ucieczka do pełnej aplikacji przez znacznik `trening:full-ui` w `sessionStorage` – tego samego używa `beforeEach` w testach e2e pełnej aplikacji. Konto, sesja, baza lokalna i połączenia (Strava, Wahoo, powiadomienia) są wspólne – `IosApp` uruchamia te same hooki co `Layout`. **Bez sesji `/i` pokazuje ekran logowania** (kod z maila; „Zobacz tylko plan, bez konta” pomija go na czas sesji przez `trening:no-account`), a `SyncBanner` na Dziś i Tygodniu zgłasza wygasłą sesję albo błąd pulla – uproszczony interfejs nigdy nie pokazuje pustych danych bez wyjaśnienia. Opis: `docs/15-interfejs-ios.md`.
- **Kadencja jest indywidualna (24.09.2026, `PROGRAM_VERSION` 2026.09.24-1)**: zawodnik ma mocne nogi i naturalnie kręci ~80 rpm, a wymuszanie 90 podnosi mu tętno przy tej samej mocy (przy stałej mocy niższa kadencja = niższe tętno i VO2). Dlatego jazdy spokojne i testy mają **80–95 rpm**, a nie 85–95; podłoga ostrzeżenia w silniku zostaje na 75 rpm. **Nie podnoś celu kadencji na zwykłych jazdach.** `Z2_CADENCE` (bloki 100–110 rpm) **wypadł z planu** – jego ograniczeniem jest układ oddechowo-sercowy, nie mięśnie (pięć dni Innsbruck–Monachium 5–9.09.2026, 539 km / 4500 m, w tym godzina przy 8,7 % nachylenia, przy kadencji 58–71 rpm – nogi wytrzymały). `Z2_FORCE` zostaje. **Wskaźnik postępu numer jeden to Pw:HR**, nie średnia moc. Uzasadnienie i dane: `docs/13` § Zmiana 24.09.2026.
- **Rower: wszystkie treningy na gravelu (Checkpoint)** od 24.09.2026; Dogma czeka na rower endurance (sezon 2027). Opis roweru liczą **dwa miejsca naraz** – `bike_suggestion` w generatorze i `bikeSuggestion` w `src/engine/calendar.ts`; rozjazd wykrywa `golden.test.ts`. Uwaga: domyślne `CdA 0,42` w `goalPower` jest dla gravela za niskie – cel „30 km/h" w Postępie będzie zaniżony, dopóki z testu FTP nie zapiszemy `ref_speed_kmh` i `ref_power_w` do `baseline_entries` (patrz `docs/13` § Zmiana 24.09.2026).
- Znaczniki dnia: `test`, `mountain_weekend`, `back_to_back`, `heat` opisują trening i wędrują przy zamianie/przeniesieniu; `deload` należy do tygodnia i zostaje.
